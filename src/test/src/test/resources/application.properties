# --- Test Properties ---
# These are dummy values to allow the test context to start.

# --- Database (H2 In-Memory) ---
# We'll use H2 (in-memory) for tests so it doesn't need a real password.
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- Gmail/Email (Mocked) ---
spring.mail.host=smtp.test.com
spring.mail.port=587
spring.mail.username=test@test.com
spring.mail.password=test_password

# --- Custom Application Properties (Mocked) ---
app.jwt.secret=a-very-long-and-secure-test-secret-key-for-jwt-256-bits-or-more
app.openweathermap.api-key=test_api_key